/**
 * First we will load all of this project's JavaScript dependencies which
 * includes React and other helpers. It's a great starting point while
 * building robust, powerful web applications using React + Laravel.
 */

require("./bootstrap");

/**
 * Next, we will create a fresh React component instance and attach it to
 * the page. Then, you may begin adding components to this application
 * or customize the JavaScript scaffolding to fit your unique needs.
 */

require("./components/Example");

// let menu = document.getElementById("option-menu");
// let btn = document.getElementById("option-btn");
// let devider = document.getElementById("costume-devider");
// let loginDropdown = document.querySelector("#navbarDropdown");

// window.addEventListener("resize", () => {
//     let totalWidth = menu.offsetWidth + devider.offsetWidth;
// });
// let totalWidth = menu.offsetWidth + devider.offsetWidth;

// menu.style.transform = "translate(0,-50px)";
// devider.style.transform = "translate(0,-50px)";
// loginDropdown.style.transform = `translate(${totalWidth}px,0px)`;

// console.log(menu.offsetWidth);

// btn.addEventListener("click", e => {
//     menu.style.transform = "translate(0,-0)";
//     devider.style.transform = "translate(0,-0)";
//     loginDropdown.style.transform = `translate(0px,0px)`;
//     btn.style.transform = `rotateZ(180deg)`;
// });

console.log("hi");
